import {useMemo,useState} from 'react'
import {Bookmark,ExternalLink,Heart,Image as ImageIcon,MapPin,X} from 'lucide-react'
import {assetUrl} from '../utils/assetUrl';import {discoverPlaces,type DiscoverCategory,type DiscoverPlace} from '../data/discover'
import {imageCredits} from '../data/imageCredits'
import type {SabahDate,SabahPlan} from '../utils/planSabah'

type Props={plan:SabahPlan;mangrove:boolean;mangroveDay:SabahDate;onOpenDay:(day:string)=>void}
const categories:DiscoverCategory[]=['吉隆坡','亚庇城市','海岛与活动']
function activityDay(activity:DiscoverPlace['activity'],plan:SabahPlan,mangrove:boolean,mangroveDay:SabahDate){if(!activity)return undefined;const entry=activity==='mangrove'&&mangrove?mangroveDay:Object.entries(plan).find(([,value])=>value===activity)?.[0];return entry?`9/${Number(entry.slice(8,10))}`:undefined}
function placeUrl(place:DiscoverPlace){return `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(place.placeName||place.english)}`}
function Picture({place,onClick}:{place:DiscoverPlace;onClick:()=>void}){const [failed,setFailed]=useState(false);const image=place.image?assetUrl(place.image):undefined;return <button className="discover-picture" onClick={onClick} aria-label={`查看${place.title}大图`}>{image&&!failed?<img src={image} alt={place.title} loading="lazy" decoding="async" onError={()=>setFailed(true)}/>:<div className="discover-placeholder"><ImageIcon size={24}/><span>{place.title}</span></div>}<span className="picture-open">查看</span></button>}
function PlaceCard({place,favorite,onFavorite,onOpenDay,onImage,plan,mangrove,mangroveDay}:{place:DiscoverPlace;favorite:boolean;onFavorite:()=>void;onOpenDay:(day:string)=>void;onImage:()=>void;plan:SabahPlan;mangrove:boolean;mangroveDay:SabahDate}){const day=place.activity?activityDay(place.activity,plan,mangrove,mangroveDay):place.day;return <article className="card discover-card"><div className="discover-card-top"><Picture place={place} onClick={onImage}/><button className={`bookmark ${favorite?'saved':''}`} onClick={onFavorite} aria-label={favorite?'取消收藏':'收藏'}>{favorite?<Heart size={16} fill="currentColor"/>:<Bookmark size={16}/>}</button></div><div className="discover-copy"><div className="micro">{place.english}</div><h3>{place.title}</h3><p className="discover-position">{place.positioning}</p><div className="tag-list">{place.tags.map(tag=><span className="tag" key={tag}>{tag}</span>)}</div><p className="discover-intro">{place.intro}</p>{place.details&&<details className="discover-details"><summary>展开小档案</summary>{place.details.map(detail=><span key={detail}>· {detail}</span>)}</details>}<div className="discover-actions">{day&&<button className="ghost-btn small" onClick={()=>onOpenDay(day)}>查看 {day} <ArrowIcon/></button>}<a className="ghost-btn small" href={placeUrl(place)} target="_blank" rel="noreferrer">导航 <ExternalLink size={12}/></a></div></div></article>}
function ArrowIcon(){return <span aria-hidden="true">↗</span>}
export function Discover({plan,mangrove,mangroveDay,onOpenDay}:Props){const [category,setCategory]=useState<DiscoverCategory>('吉隆坡');const [view,setView]=useState<'cards'|'gallery'>('cards');const [onlySaved,setOnlySaved]=useState(false);const [saved,setSaved]=useState<string[]>(()=>{try{return JSON.parse(localStorage.getItem('trip-favorites')||'[]')}catch{return []}});const [lightbox,setLightbox]=useState<DiscoverPlace|null>(null);const [lightboxFailed,setLightboxFailed]=useState(false);const filtered=useMemo(()=>discoverPlaces.filter(place=>(onlySaved?saved.includes(place.id):place.category===category)),[category,onlySaved,saved]);const lightboxDay=lightbox?.activity?activityDay(lightbox.activity,plan,mangrove,mangroveDay):lightbox?.day;const toggle=(id:string)=>setSaved(prev=>{const next=prev.includes(id)?prev.filter(item=>item!==id):[...prev,id];localStorage.setItem('trip-favorites',JSON.stringify(next));return next});return <section className="section" id="discover"><div className="section-head"><div><div className="section-kicker">12 · discover</div><h2>景点与活动图鉴</h2></div><div className="section-note">不想读完整攻略时，先看“这个地方到底是什么”。图片、定位和动态行程入口都收在这里。</div></div><div className="discover-toolbar"><div className="discover-tabs" role="tablist">{categories.map(item=><button key={item} className={category===item&&!onlySaved?'active':''} onClick={()=>{setCategory(item);setOnlySaved(false)}} role="tab">{item}</button>)}<button className={onlySaved?'active':''} onClick={()=>setOnlySaved(true)}><Heart size={14}/>我的收藏 {saved.length}</button></div><div className="view-toggle"><button className={view==='cards'?'active':''} onClick={()=>setView('cards')}>卡片</button><button className={view==='gallery'?'active':''} onClick={()=>setView('gallery')}>Gallery</button></div></div>{filtered.length===0?<div className="card empty-state">还没有收藏。点景点卡右上角的书签，把想去、想吃、想拍照的内容留下来。</div>:<div className={view==='cards'?'discover-grid':'gallery-grid'}>{filtered.map(place=><PlaceCard key={place.id} place={place} favorite={saved.includes(place.id)} onFavorite={()=>toggle(place.id)} onOpenDay={onOpenDay} onImage={()=>{setLightboxFailed(false);setLightbox(place)}} plan={plan} mangrove={mangrove} mangroveDay={mangroveDay}/>)}</div>}<details className="credits"><summary>图片来源</summary>{imageCredits.map(credit=><a href={credit.original} target="_blank" rel="noreferrer" key={credit.file}>{credit.file} · {credit.author} · {credit.license}</a>)}</details>{lightbox&&<div className="lightbox" role="dialog" aria-modal="true" onClick={()=>setLightbox(null)}><button className="lightbox-close" onClick={()=>setLightbox(null)} aria-label="关闭"><X/></button><div className="lightbox-content" onClick={event=>event.stopPropagation()}>{lightbox.image&&!lightboxFailed?<img src={assetUrl(lightbox.image)} alt={lightbox.title} decoding="async" onError={()=>setLightboxFailed(true)}/>:<div className="discover-placeholder large"><ImageIcon size={42}/><span>{lightbox.title}</span></div>}<div className="lightbox-meta"><div className="micro">{lightbox.english}</div><h3>{lightbox.title}</h3><p>{lightbox.intro}</p>{lightboxDay&&<button className="ghost-btn small" onClick={()=>{setLightbox(null);onOpenDay(lightboxDay)}}>查看 {lightboxDay} 行程 ↗</button>}</div></div></div>}</section>}



